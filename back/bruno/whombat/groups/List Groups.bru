meta {
  name: List Groups
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/groups/?limit=50&offset=0
  body: none
  auth: none
}

params:query {
  limit: 50
  offset: 0
}

docs {
  List all groups (admin only).

  This endpoint requires admin privileges and returns a paginated
  list of all groups in the system.

  Query Parameters:
  - limit: Number of results to return (default: 50)
  - offset: Number of results to skip (default: 0)
}
