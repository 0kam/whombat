meta {
  name: Update Audio Directory
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/setup/audio_dir/
  body: json
  auth: none
}

body:json {
  {
    "audio_dir": "/home/user/audio"
  }
}

docs {
  Persist a new audio directory in the application settings.

  This endpoint updates the audio directory path in the application
  settings file. The provided path must be an existing directory.

  Request Body:
  - audio_dir: Path to the audio directory (must exist)
}
