meta {
  name: Get Audio Directory
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/setup/audio_dir/
  body: none
  auth: none
}

docs {
  Return the currently configured audio directory.

  This endpoint is used during initial application setup to check
  the current audio directory configuration.
}
