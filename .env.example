# ============================================
# Whombat Configuration Template
# ============================================
# Copy this file to .env and modify the values as needed
# cp .env.example .env

# ============================================
# PostgreSQL Database Configuration
# ============================================
POSTGRES_DB=whombat
POSTGRES_USER=postgres
POSTGRES_PASSWORD=whombat

# ============================================
# Network Configuration
# ============================================
# WHOMBAT_DOMAIN: The domain or IP address where Whombat will be accessed
# - For remote access: Set to your server's IP (e.g., 192.168.1.100) or domain (e.g., whombat.example.com)
# - For local only: Use "localhost"
# - This is used for browser redirects and authentication cookies
WHOMBAT_DOMAIN=localhost

# WHOMBAT_HOST: Network interface to bind to
# - Use "0.0.0.0" to allow remote access from any network interface (recommended for remote servers)
# - Use "localhost" or "127.0.0.1" for local-only access
WHOMBAT_HOST=0.0.0.0

# WHOMBAT_PORT: Backend server port
# - Default: 5000
WHOMBAT_PORT=5000

# WHOMBAT_FRONTEND_PORT: Frontend server port
# - Default: 3000
WHOMBAT_FRONTEND_PORT=3000

# WHOMBAT_PROTOCOL: Protocol for accessing Whombat
# - Use "http" for development or when not using SSL
# - Use "https" if you have SSL certificates configured
WHOMBAT_PROTOCOL=http

# ============================================
# Audio Directory Configuration (REQUIRED)
# ============================================
# Path on the SERVER where audio files are stored
# IMPORTANT: This MUST be set before starting Whombat
# Example: WHOMBAT_AUDIO_DIR=/home/user/audio_data
WHOMBAT_AUDIO_DIR=/path/to/audio/files

# ============================================
# Development Mode
# ============================================
# Set to true for development mode (auto-reload, verbose logging)
# - true: Development mode with auto-reload and detailed logging
# - false: Production mode (recommended for normal use)
WHOMBAT_DEV=false

# ============================================
# CORS Configuration (Advanced)
# ============================================
# CORS origins are automatically generated based on WHOMBAT_DOMAIN and WHOMBAT_FRONTEND_PORT
# Only set this if you need custom CORS origins for advanced configurations
# Format: JSON array of allowed origins
# Example: WHOMBAT_CORS_ORIGINS='["http://localhost:3000","http://192.168.1.100:3000","https://whombat.example.com:3000"]'
# WHOMBAT_CORS_ORIGINS=

# ============================================
# Example Configurations
# ============================================

# Example 1: Local development (default)
# WHOMBAT_DOMAIN=localhost
# WHOMBAT_HOST=localhost
# WHOMBAT_PORT=5000
# WHOMBAT_FRONTEND_PORT=3000
# WHOMBAT_PROTOCOL=http
# WHOMBAT_DEV=true

# Example 2: Remote server with IP address
# WHOMBAT_DOMAIN=192.168.1.100
# WHOMBAT_HOST=0.0.0.0
# WHOMBAT_PORT=5000
# WHOMBAT_FRONTEND_PORT=3000
# WHOMBAT_PROTOCOL=http
# WHOMBAT_DEV=false

# Example 3: Remote server with domain name and HTTPS
# WHOMBAT_DOMAIN=whombat.example.com
# WHOMBAT_HOST=0.0.0.0
# WHOMBAT_PORT=5000
# WHOMBAT_FRONTEND_PORT=3000
# WHOMBAT_PROTOCOL=https
# WHOMBAT_DEV=false
# WHOMBAT_AUTH_COOKIE_SECURE=true
